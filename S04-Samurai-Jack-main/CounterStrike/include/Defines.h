#ifndef DEFINES_H_
#define DEFINES_H_

enum PistolType {
  GLOCK, DESERT_EAGLE
};

enum GlockDefines {
  ROUNDS_PER_FIRE = 3
};

enum PlayerStatus {
    ACTIVE, PASSIVE
};

enum PlayerDefines {
  PLAYER_ONE, PLAYER_TWO, PLAYERS_COUNT
};

#endif /* DEFINES_H_ */
